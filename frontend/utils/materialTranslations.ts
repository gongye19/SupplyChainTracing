/**
 * 物料名称翻译工具（已改为直接返回英文）
 * @param materialName 物料名称
 * @returns 物料名称（保持英文）
 */
export function translateMaterial(materialName: string): string {
  // 直接返回英文，不再翻译
  return materialName;
}

/**
 * 批量翻译物料名称列表（已改为直接返回英文）
 * @param materials 物料名称数组
 * @returns 物料名称数组（保持英文）
 */
export function translateMaterials(materials: string[]): string[] {
  // 直接返回英文，不再翻译
  return materials;
}

